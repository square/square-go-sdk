// Code generated by Fern. DO NOT EDIT.

package bookings_test

import (
	bytes "bytes"
	context "context"
	json "encoding/json"
	square "github.com/square/square-go-sdk/v3"
	client "github.com/square/square-go-sdk/v3/client"
	option "github.com/square/square-go-sdk/v3/option"
	require "github.com/stretchr/testify/require"
	http "net/http"
	os "os"
	testing "testing"
)

func VerifyRequestCount(
	t *testing.T,
	testId string,
	method string,
	urlPath string,
	queryParams map[string]string,
	expected int,
) {
	wiremockURL := os.Getenv("WIREMOCK_URL")
	if wiremockURL == "" {
		wiremockURL = "http://localhost:8080"
	}
	WiremockAdminURL := wiremockURL + "/__admin"
	var reqBody bytes.Buffer
	reqBody.WriteString(`{"method":"`)
	reqBody.WriteString(method)
	reqBody.WriteString(`","urlPath":"`)
	reqBody.WriteString(urlPath)
	reqBody.WriteString(`","headers":{"X-Test-Id":{"equalTo":"`)
	reqBody.WriteString(testId)
	reqBody.WriteString(`"}}`)
	if len(queryParams) > 0 {
		reqBody.WriteString(`,"queryParameters":{`)
		first := true
		for key, value := range queryParams {
			if !first {
				reqBody.WriteString(",")
			}
			reqBody.WriteString(`"`)
			reqBody.WriteString(key)
			reqBody.WriteString(`":{"equalTo":"`)
			reqBody.WriteString(value)
			reqBody.WriteString(`"}`)
			first = false
		}
		reqBody.WriteString("}")
	}
	reqBody.WriteString("}")
	resp, err := http.Post(WiremockAdminURL+"/requests/find", "application/json", &reqBody)
	require.NoError(t, err)
	var result struct {
		Requests []interface{} `json:"requests"`
	}
	json.NewDecoder(resp.Body).Decode(&result)
	require.Equal(t, expected, len(result.Requests))
}

func TestBookingsListWithWireMock(
	t *testing.T,
) {
	WireMockBaseURL := os.Getenv("WIREMOCK_URL")
	if WireMockBaseURL == "" {
		WireMockBaseURL = "http://localhost:8080"
	}
	client := client.NewClient(
		option.WithBaseURL(WireMockBaseURL),
	)
	request := &square.ListBookingsRequest{
		Limit: square.Int(
			1,
		),
		Cursor: square.String(
			"cursor",
		),
		CustomerID: square.String(
			"customer_id",
		),
		TeamMemberID: square.String(
			"team_member_id",
		),
		LocationID: square.String(
			"location_id",
		),
		StartAtMin: square.String(
			"start_at_min",
		),
		StartAtMax: square.String(
			"start_at_max",
		),
	}
	_, invocationErr := client.Bookings.List(
		context.TODO(),
		request,
		option.WithHTTPHeader(
			http.Header{"X-Test-Id": []string{"TestBookingsListWithWireMock"}},
		),
	)

	require.NoError(t, invocationErr, "Client method call should succeed")
	VerifyRequestCount(t, "TestBookingsListWithWireMock", "GET", "/v2/bookings", map[string]string{"limit": "1", "cursor": "cursor", "customer_id": "customer_id", "team_member_id": "team_member_id", "location_id": "location_id", "start_at_min": "start_at_min", "start_at_max": "start_at_max"}, 1)
}

func TestBookingsCreateWithWireMock(
	t *testing.T,
) {
	WireMockBaseURL := os.Getenv("WIREMOCK_URL")
	if WireMockBaseURL == "" {
		WireMockBaseURL = "http://localhost:8080"
	}
	client := client.NewClient(
		option.WithBaseURL(WireMockBaseURL),
	)
	request := &square.CreateBookingRequest{
		Booking: &square.Booking{},
	}
	_, invocationErr := client.Bookings.Create(
		context.TODO(),
		request,
		option.WithHTTPHeader(
			http.Header{"X-Test-Id": []string{"TestBookingsCreateWithWireMock"}},
		),
	)

	require.NoError(t, invocationErr, "Client method call should succeed")
	VerifyRequestCount(t, "TestBookingsCreateWithWireMock", "POST", "/v2/bookings", nil, 1)
}

func TestBookingsSearchAvailabilityWithWireMock(
	t *testing.T,
) {
	WireMockBaseURL := os.Getenv("WIREMOCK_URL")
	if WireMockBaseURL == "" {
		WireMockBaseURL = "http://localhost:8080"
	}
	client := client.NewClient(
		option.WithBaseURL(WireMockBaseURL),
	)
	request := &square.SearchAvailabilityRequest{
		Query: &square.SearchAvailabilityQuery{
			Filter: &square.SearchAvailabilityFilter{
				StartAtRange: &square.TimeRange{},
			},
		},
	}
	_, invocationErr := client.Bookings.SearchAvailability(
		context.TODO(),
		request,
		option.WithHTTPHeader(
			http.Header{"X-Test-Id": []string{"TestBookingsSearchAvailabilityWithWireMock"}},
		),
	)

	require.NoError(t, invocationErr, "Client method call should succeed")
	VerifyRequestCount(t, "TestBookingsSearchAvailabilityWithWireMock", "POST", "/v2/bookings/availability/search", nil, 1)
}

func TestBookingsBulkRetrieveBookingsWithWireMock(
	t *testing.T,
) {
	WireMockBaseURL := os.Getenv("WIREMOCK_URL")
	if WireMockBaseURL == "" {
		WireMockBaseURL = "http://localhost:8080"
	}
	client := client.NewClient(
		option.WithBaseURL(WireMockBaseURL),
	)
	request := &square.BulkRetrieveBookingsRequest{
		BookingIDs: []string{
			"booking_ids",
		},
	}
	_, invocationErr := client.Bookings.BulkRetrieveBookings(
		context.TODO(),
		request,
		option.WithHTTPHeader(
			http.Header{"X-Test-Id": []string{"TestBookingsBulkRetrieveBookingsWithWireMock"}},
		),
	)

	require.NoError(t, invocationErr, "Client method call should succeed")
	VerifyRequestCount(t, "TestBookingsBulkRetrieveBookingsWithWireMock", "POST", "/v2/bookings/bulk-retrieve", nil, 1)
}

func TestBookingsGetBusinessProfileWithWireMock(
	t *testing.T,
) {
	WireMockBaseURL := os.Getenv("WIREMOCK_URL")
	if WireMockBaseURL == "" {
		WireMockBaseURL = "http://localhost:8080"
	}
	client := client.NewClient(
		option.WithBaseURL(WireMockBaseURL),
	)
	_, invocationErr := client.Bookings.GetBusinessProfile(
		context.TODO(),
		option.WithHTTPHeader(
			http.Header{"X-Test-Id": []string{"TestBookingsGetBusinessProfileWithWireMock"}},
		),
	)

	require.NoError(t, invocationErr, "Client method call should succeed")
	VerifyRequestCount(t, "TestBookingsGetBusinessProfileWithWireMock", "GET", "/v2/bookings/business-booking-profile", nil, 1)
}

func TestBookingsRetrieveLocationBookingProfileWithWireMock(
	t *testing.T,
) {
	WireMockBaseURL := os.Getenv("WIREMOCK_URL")
	if WireMockBaseURL == "" {
		WireMockBaseURL = "http://localhost:8080"
	}
	client := client.NewClient(
		option.WithBaseURL(WireMockBaseURL),
	)
	request := &square.RetrieveLocationBookingProfileRequest{
		LocationID: "location_id",
	}
	_, invocationErr := client.Bookings.RetrieveLocationBookingProfile(
		context.TODO(),
		request,
		option.WithHTTPHeader(
			http.Header{"X-Test-Id": []string{"TestBookingsRetrieveLocationBookingProfileWithWireMock"}},
		),
	)

	require.NoError(t, invocationErr, "Client method call should succeed")
	VerifyRequestCount(t, "TestBookingsRetrieveLocationBookingProfileWithWireMock", "GET", "/v2/bookings/location-booking-profiles/location_id", nil, 1)
}

func TestBookingsBulkRetrieveTeamMemberBookingProfilesWithWireMock(
	t *testing.T,
) {
	WireMockBaseURL := os.Getenv("WIREMOCK_URL")
	if WireMockBaseURL == "" {
		WireMockBaseURL = "http://localhost:8080"
	}
	client := client.NewClient(
		option.WithBaseURL(WireMockBaseURL),
	)
	request := &square.BulkRetrieveTeamMemberBookingProfilesRequest{
		TeamMemberIDs: []string{
			"team_member_ids",
		},
	}
	_, invocationErr := client.Bookings.BulkRetrieveTeamMemberBookingProfiles(
		context.TODO(),
		request,
		option.WithHTTPHeader(
			http.Header{"X-Test-Id": []string{"TestBookingsBulkRetrieveTeamMemberBookingProfilesWithWireMock"}},
		),
	)

	require.NoError(t, invocationErr, "Client method call should succeed")
	VerifyRequestCount(t, "TestBookingsBulkRetrieveTeamMemberBookingProfilesWithWireMock", "POST", "/v2/bookings/team-member-booking-profiles/bulk-retrieve", nil, 1)
}

func TestBookingsGetWithWireMock(
	t *testing.T,
) {
	WireMockBaseURL := os.Getenv("WIREMOCK_URL")
	if WireMockBaseURL == "" {
		WireMockBaseURL = "http://localhost:8080"
	}
	client := client.NewClient(
		option.WithBaseURL(WireMockBaseURL),
	)
	request := &square.GetBookingsRequest{
		BookingID: "booking_id",
	}
	_, invocationErr := client.Bookings.Get(
		context.TODO(),
		request,
		option.WithHTTPHeader(
			http.Header{"X-Test-Id": []string{"TestBookingsGetWithWireMock"}},
		),
	)

	require.NoError(t, invocationErr, "Client method call should succeed")
	VerifyRequestCount(t, "TestBookingsGetWithWireMock", "GET", "/v2/bookings/booking_id", nil, 1)
}

func TestBookingsUpdateWithWireMock(
	t *testing.T,
) {
	WireMockBaseURL := os.Getenv("WIREMOCK_URL")
	if WireMockBaseURL == "" {
		WireMockBaseURL = "http://localhost:8080"
	}
	client := client.NewClient(
		option.WithBaseURL(WireMockBaseURL),
	)
	request := &square.UpdateBookingRequest{
		BookingID: "booking_id",
		Booking:   &square.Booking{},
	}
	_, invocationErr := client.Bookings.Update(
		context.TODO(),
		request,
		option.WithHTTPHeader(
			http.Header{"X-Test-Id": []string{"TestBookingsUpdateWithWireMock"}},
		),
	)

	require.NoError(t, invocationErr, "Client method call should succeed")
	VerifyRequestCount(t, "TestBookingsUpdateWithWireMock", "PUT", "/v2/bookings/booking_id", nil, 1)
}

func TestBookingsCancelWithWireMock(
	t *testing.T,
) {
	WireMockBaseURL := os.Getenv("WIREMOCK_URL")
	if WireMockBaseURL == "" {
		WireMockBaseURL = "http://localhost:8080"
	}
	client := client.NewClient(
		option.WithBaseURL(WireMockBaseURL),
	)
	request := &square.CancelBookingRequest{
		BookingID: "booking_id",
	}
	_, invocationErr := client.Bookings.Cancel(
		context.TODO(),
		request,
		option.WithHTTPHeader(
			http.Header{"X-Test-Id": []string{"TestBookingsCancelWithWireMock"}},
		),
	)

	require.NoError(t, invocationErr, "Client method call should succeed")
	VerifyRequestCount(t, "TestBookingsCancelWithWireMock", "POST", "/v2/bookings/booking_id/cancel", nil, 1)
}

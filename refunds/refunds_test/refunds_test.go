// Code generated by Fern. DO NOT EDIT.

package refunds_test

import (
	bytes "bytes"
	context "context"
	json "encoding/json"
	square "github.com/square/square-go-sdk/v3"
	client "github.com/square/square-go-sdk/v3/client"
	option "github.com/square/square-go-sdk/v3/option"
	require "github.com/stretchr/testify/require"
	http "net/http"
	os "os"
	testing "testing"
)

func VerifyRequestCount(
	t *testing.T,
	testId string,
	method string,
	urlPath string,
	queryParams map[string]string,
	expected int,
) {
	wiremockURL := os.Getenv("WIREMOCK_URL")
	if wiremockURL == "" {
		wiremockURL = "http://localhost:8080"
	}
	WiremockAdminURL := wiremockURL + "/__admin"
	var reqBody bytes.Buffer
	reqBody.WriteString(`{"method":"`)
	reqBody.WriteString(method)
	reqBody.WriteString(`","urlPath":"`)
	reqBody.WriteString(urlPath)
	reqBody.WriteString(`","headers":{"X-Test-Id":{"equalTo":"`)
	reqBody.WriteString(testId)
	reqBody.WriteString(`"}}`)
	if len(queryParams) > 0 {
		reqBody.WriteString(`,"queryParameters":{`)
		first := true
		for key, value := range queryParams {
			if !first {
				reqBody.WriteString(",")
			}
			reqBody.WriteString(`"`)
			reqBody.WriteString(key)
			reqBody.WriteString(`":{"equalTo":"`)
			reqBody.WriteString(value)
			reqBody.WriteString(`"}`)
			first = false
		}
		reqBody.WriteString("}")
	}
	reqBody.WriteString("}")
	resp, err := http.Post(WiremockAdminURL+"/requests/find", "application/json", &reqBody)
	require.NoError(t, err)
	var result struct {
		Requests []interface{} `json:"requests"`
	}
	json.NewDecoder(resp.Body).Decode(&result)
	require.Equal(t, expected, len(result.Requests))
}

func TestRefundsListWithWireMock(
	t *testing.T,
) {
	WireMockBaseURL := os.Getenv("WIREMOCK_URL")
	if WireMockBaseURL == "" {
		WireMockBaseURL = "http://localhost:8080"
	}
	client := client.NewClient(
		option.WithBaseURL(WireMockBaseURL),
	)
	request := &square.ListRefundsRequest{
		BeginTime: square.String(
			"begin_time",
		),
		EndTime: square.String(
			"end_time",
		),
		SortOrder: square.String(
			"sort_order",
		),
		Cursor: square.String(
			"cursor",
		),
		LocationID: square.String(
			"location_id",
		),
		Status: square.String(
			"status",
		),
		SourceType: square.String(
			"source_type",
		),
		Limit: square.Int(
			1,
		),
		UpdatedAtBeginTime: square.String(
			"updated_at_begin_time",
		),
		UpdatedAtEndTime: square.String(
			"updated_at_end_time",
		),
		SortField: square.ListPaymentRefundsRequestSortFieldCreatedAt.Ptr(),
	}
	_, invocationErr := client.Refunds.List(
		context.TODO(),
		request,
		option.WithHTTPHeader(
			http.Header{"X-Test-Id": []string{"TestRefundsListWithWireMock"}},
		),
	)

	require.NoError(t, invocationErr, "Client method call should succeed")
	VerifyRequestCount(t, "TestRefundsListWithWireMock", "GET", "/v2/refunds", map[string]string{"begin_time": "begin_time", "end_time": "end_time", "sort_order": "sort_order", "cursor": "cursor", "location_id": "location_id", "status": "status", "source_type": "source_type", "limit": "1", "updated_at_begin_time": "updated_at_begin_time", "updated_at_end_time": "updated_at_end_time", "sort_field": "CREATED_AT"}, 1)
}

func TestRefundsRefundPaymentWithWireMock(
	t *testing.T,
) {
	WireMockBaseURL := os.Getenv("WIREMOCK_URL")
	if WireMockBaseURL == "" {
		WireMockBaseURL = "http://localhost:8080"
	}
	client := client.NewClient(
		option.WithBaseURL(WireMockBaseURL),
	)
	request := &square.RefundPaymentRequest{
		IdempotencyKey: "9b7f2dcf-49da-4411-b23e-a2d6af21333a",
		AmountMoney: &square.Money{
			Amount: square.Int64(
				int64(1000),
			),
			Currency: square.CurrencyUsd.Ptr(),
		},
		AppFeeMoney: &square.Money{
			Amount: square.Int64(
				int64(10),
			),
			Currency: square.CurrencyUsd.Ptr(),
		},
		PaymentID: square.String(
			"R2B3Z8WMVt3EAmzYWLZvz7Y69EbZY",
		),
		Reason: square.String(
			"Example",
		),
	}
	_, invocationErr := client.Refunds.RefundPayment(
		context.TODO(),
		request,
		option.WithHTTPHeader(
			http.Header{"X-Test-Id": []string{"TestRefundsRefundPaymentWithWireMock"}},
		),
	)

	require.NoError(t, invocationErr, "Client method call should succeed")
	VerifyRequestCount(t, "TestRefundsRefundPaymentWithWireMock", "POST", "/v2/refunds", nil, 1)
}

func TestRefundsGetWithWireMock(
	t *testing.T,
) {
	WireMockBaseURL := os.Getenv("WIREMOCK_URL")
	if WireMockBaseURL == "" {
		WireMockBaseURL = "http://localhost:8080"
	}
	client := client.NewClient(
		option.WithBaseURL(WireMockBaseURL),
	)
	request := &square.GetRefundsRequest{
		RefundID: "refund_id",
	}
	_, invocationErr := client.Refunds.Get(
		context.TODO(),
		request,
		option.WithHTTPHeader(
			http.Header{"X-Test-Id": []string{"TestRefundsGetWithWireMock"}},
		),
	)

	require.NoError(t, invocationErr, "Client method call should succeed")
	VerifyRequestCount(t, "TestRefundsGetWithWireMock", "GET", "/v2/refunds/refund_id", nil, 1)
}

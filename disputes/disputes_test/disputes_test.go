// Code generated by Fern. DO NOT EDIT.

package disputes_test

import (
	bytes "bytes"
	context "context"
	json "encoding/json"
	square "github.com/square/square-go-sdk/v3"
	client "github.com/square/square-go-sdk/v3/client"
	option "github.com/square/square-go-sdk/v3/option"
	require "github.com/stretchr/testify/require"
	http "net/http"
	os "os"
	strings "strings"
	testing "testing"
)

func VerifyRequestCount(
	t *testing.T,
	testId string,
	method string,
	urlPath string,
	queryParams map[string]string,
	expected int,
) {
	wiremockURL := os.Getenv("WIREMOCK_URL")
	if wiremockURL == "" {
		wiremockURL = "http://localhost:8080"
	}
	WiremockAdminURL := wiremockURL + "/__admin"
	var reqBody bytes.Buffer
	reqBody.WriteString(`{"method":"`)
	reqBody.WriteString(method)
	reqBody.WriteString(`","urlPath":"`)
	reqBody.WriteString(urlPath)
	reqBody.WriteString(`","headers":{"X-Test-Id":{"equalTo":"`)
	reqBody.WriteString(testId)
	reqBody.WriteString(`"}}`)
	if len(queryParams) > 0 {
		reqBody.WriteString(`,"queryParameters":{`)
		first := true
		for key, value := range queryParams {
			if !first {
				reqBody.WriteString(",")
			}
			reqBody.WriteString(`"`)
			reqBody.WriteString(key)
			reqBody.WriteString(`":{"equalTo":"`)
			reqBody.WriteString(value)
			reqBody.WriteString(`"}`)
			first = false
		}
		reqBody.WriteString("}")
	}
	reqBody.WriteString("}")
	resp, err := http.Post(WiremockAdminURL+"/requests/find", "application/json", &reqBody)
	require.NoError(t, err)
	var result struct {
		Requests []interface{} `json:"requests"`
	}
	json.NewDecoder(resp.Body).Decode(&result)
	require.Equal(t, expected, len(result.Requests))
}

func TestDisputesListWithWireMock(
	t *testing.T,
) {
	WireMockBaseURL := os.Getenv("WIREMOCK_URL")
	if WireMockBaseURL == "" {
		WireMockBaseURL = "http://localhost:8080"
	}
	client := client.NewClient(
		option.WithBaseURL(WireMockBaseURL),
	)
	request := &square.ListDisputesRequest{
		Cursor: square.String(
			"cursor",
		),
		States: square.DisputeStateInquiryEvidenceRequired.Ptr(),
		LocationID: square.String(
			"location_id",
		),
	}
	_, invocationErr := client.Disputes.List(
		context.TODO(),
		request,
		option.WithHTTPHeader(
			http.Header{"X-Test-Id": []string{"TestDisputesListWithWireMock"}},
		),
	)

	require.NoError(t, invocationErr, "Client method call should succeed")
	VerifyRequestCount(t, "TestDisputesListWithWireMock", "GET", "/v2/disputes", map[string]string{"cursor": "cursor", "states": "INQUIRY_EVIDENCE_REQUIRED", "location_id": "location_id"}, 1)
}

func TestDisputesGetWithWireMock(
	t *testing.T,
) {
	WireMockBaseURL := os.Getenv("WIREMOCK_URL")
	if WireMockBaseURL == "" {
		WireMockBaseURL = "http://localhost:8080"
	}
	client := client.NewClient(
		option.WithBaseURL(WireMockBaseURL),
	)
	request := &square.GetDisputesRequest{
		DisputeID: "dispute_id",
	}
	_, invocationErr := client.Disputes.Get(
		context.TODO(),
		request,
		option.WithHTTPHeader(
			http.Header{"X-Test-Id": []string{"TestDisputesGetWithWireMock"}},
		),
	)

	require.NoError(t, invocationErr, "Client method call should succeed")
	VerifyRequestCount(t, "TestDisputesGetWithWireMock", "GET", "/v2/disputes/dispute_id", nil, 1)
}

func TestDisputesAcceptWithWireMock(
	t *testing.T,
) {
	WireMockBaseURL := os.Getenv("WIREMOCK_URL")
	if WireMockBaseURL == "" {
		WireMockBaseURL = "http://localhost:8080"
	}
	client := client.NewClient(
		option.WithBaseURL(WireMockBaseURL),
	)
	request := &square.AcceptDisputesRequest{
		DisputeID: "dispute_id",
	}
	_, invocationErr := client.Disputes.Accept(
		context.TODO(),
		request,
		option.WithHTTPHeader(
			http.Header{"X-Test-Id": []string{"TestDisputesAcceptWithWireMock"}},
		),
	)

	require.NoError(t, invocationErr, "Client method call should succeed")
	VerifyRequestCount(t, "TestDisputesAcceptWithWireMock", "POST", "/v2/disputes/dispute_id/accept", nil, 1)
}

func TestDisputesCreateEvidenceFileWithWireMock(
	t *testing.T,
) {
	WireMockBaseURL := os.Getenv("WIREMOCK_URL")
	if WireMockBaseURL == "" {
		WireMockBaseURL = "http://localhost:8080"
	}
	client := client.NewClient(
		option.WithBaseURL(WireMockBaseURL),
	)
	request := &square.CreateEvidenceFileDisputesRequest{
		DisputeID: "dispute_id",
		ImageFile: strings.NewReader(
			"",
		),
	}
	_, invocationErr := client.Disputes.CreateEvidenceFile(
		context.TODO(),
		request,
		option.WithHTTPHeader(
			http.Header{"X-Test-Id": []string{"TestDisputesCreateEvidenceFileWithWireMock"}},
		),
	)

	require.NoError(t, invocationErr, "Client method call should succeed")
	VerifyRequestCount(t, "TestDisputesCreateEvidenceFileWithWireMock", "POST", "/v2/disputes/dispute_id/evidence-files", nil, 1)
}

func TestDisputesCreateEvidenceTextWithWireMock(
	t *testing.T,
) {
	WireMockBaseURL := os.Getenv("WIREMOCK_URL")
	if WireMockBaseURL == "" {
		WireMockBaseURL = "http://localhost:8080"
	}
	client := client.NewClient(
		option.WithBaseURL(WireMockBaseURL),
	)
	request := &square.CreateDisputeEvidenceTextRequest{
		DisputeID:      "dispute_id",
		IdempotencyKey: "ed3ee3933d946f1514d505d173c82648",
		EvidenceType:   square.DisputeEvidenceTypeTrackingNumber.Ptr(),
		EvidenceText:   "1Z8888888888888888",
	}
	_, invocationErr := client.Disputes.CreateEvidenceText(
		context.TODO(),
		request,
		option.WithHTTPHeader(
			http.Header{"X-Test-Id": []string{"TestDisputesCreateEvidenceTextWithWireMock"}},
		),
	)

	require.NoError(t, invocationErr, "Client method call should succeed")
	VerifyRequestCount(t, "TestDisputesCreateEvidenceTextWithWireMock", "POST", "/v2/disputes/dispute_id/evidence-text", nil, 1)
}

func TestDisputesSubmitEvidenceWithWireMock(
	t *testing.T,
) {
	WireMockBaseURL := os.Getenv("WIREMOCK_URL")
	if WireMockBaseURL == "" {
		WireMockBaseURL = "http://localhost:8080"
	}
	client := client.NewClient(
		option.WithBaseURL(WireMockBaseURL),
	)
	request := &square.SubmitEvidenceDisputesRequest{
		DisputeID: "dispute_id",
	}
	_, invocationErr := client.Disputes.SubmitEvidence(
		context.TODO(),
		request,
		option.WithHTTPHeader(
			http.Header{"X-Test-Id": []string{"TestDisputesSubmitEvidenceWithWireMock"}},
		),
	)

	require.NoError(t, invocationErr, "Client method call should succeed")
	VerifyRequestCount(t, "TestDisputesSubmitEvidenceWithWireMock", "POST", "/v2/disputes/dispute_id/submit-evidence", nil, 1)
}
